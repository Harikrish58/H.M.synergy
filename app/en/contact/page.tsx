import ContactForm from "@/components/ContactForm";

export const metadata = {
  title: "Contact — H&M Synergy",
  description: "Write to us — we reply within 24 business hours.",
};

export default function Page() {
  return <ContactForm />;
}
